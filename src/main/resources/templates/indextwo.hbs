{{#partial "content"}}

    <h1>Hero list!</h1>

    {{#if posts}}
        {{#each posts }}
            <p>Current post:</p>
            <p>{{ content }} <a href="/hero/{{id}}">read more...</a></p>
        {{/each}}
    {{else}}
        <p>No posts!</p>
    {{/if}}
{{/partial}}

{{> layout.hbs}}